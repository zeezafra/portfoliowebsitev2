// Phase 1 route stub — real content for this section is a later phase
// (see references/roadmap.md). Exists now so Phase 2's nav has a valid
// route to link to.
export default function ContactPage() {
  return (
    <section className="flex min-h-[calc(100vh-0px)] flex-col items-start justify-center gap-2 px-6 py-16 md:px-12">
      <h1 className="text-3xl font-semibold text-heading">Contact</h1>
      <p className="text-foreground/70">Content coming in a later phase.</p>
    </section>
  );
}
